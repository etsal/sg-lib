enclave {
    from "sg.edl" import *;
trusted {
        /* define ECALLs here. */
        public int generate_random_number(void);
        public void init();
        public void connect_cluster();
        public void recieve_cluster_connections();
        public void poll_and_process_updates();
        public int verify_cluster_connections();
    };

    untrusted {
        /* define OCALLs here. */
        void ocall_print([in, string]const char* str);
        
    };
};
