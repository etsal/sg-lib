enclave {
    from "sg_interface.edl" import *;
    
    trusted {   
    };
    
    untrusted {
        /* define OCALLs here. */
        void ocall_print([in, string]const char* str);
        
    };
};
