#ifndef SG_NETWORK
#define SG_NETWORK

#include <stdint.h>

// This struct is used to pass the fds to the application code, SGX is weird about passing integer arrays

#define MAX_CLUSTER_CONNECTIONS 5
#define MAX_FD_SGX 500
#endif
