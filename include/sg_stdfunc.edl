enclave {
  trusted{

  };

  untrusted {
    void ocall_exit(int s);
    void ocall_eprintf([ in, string ] const char *str);
    void ocall_lprintf([ in, string ] const char *str);
    void ocall_print_string([in, string] const char *str);
  };
};
